const AuthValidation = require('./auth.validation');
const BuildingValidation = require('./building.validation');
const BookingValidation = require('./booking.validation');

module.exports = {
    AuthValidation,
    BuildingValidation,
    BookingValidation
}; 